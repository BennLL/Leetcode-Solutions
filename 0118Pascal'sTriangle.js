/**
 * @param {number} numRows
 * @return {number[][]}
 */
var generate = function(numRows) {
    const arr = [][];
    for(x = 1, x < numRows; x++){

    }
};

numRows = 5;
console.log(generate(numRows));